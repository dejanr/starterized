'use strict';

var sinon = require('sinon');
var expect = require('expect.js');

describe('commands/compile', function() {
  var sandbox;

  beforeEach(function() {
    sandbox = sinon.sandbox.create();
  });

  afterEach(function() {
    sandbox.restore();
  });

  describe('Compile', function() {

  });
});
